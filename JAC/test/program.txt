while (b != 0) {
    if (a > b) {
        a = a-b;
    }
    else { // This is a oomment, hopefully ignored.
    /*
    * What are the odds of this working?
    */
        b = b - a;
    }
}
return a;

'a'
"aa"

